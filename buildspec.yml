version: 0.2

phases:
  install:
  pre_build:
  build:
    commands:
    - npm install
    - npm run lint
    - npm run test
    - npm run build
    - zip -r dist.zip dist
    - ls -lart
  post_build:
artifacts:
  files:
  - 'dist.zip'
  - name ngm-$(date +%Y-%m-%d)
  discard-paths: yes
