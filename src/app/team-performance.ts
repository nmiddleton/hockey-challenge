export class TeamPerformance {
  id: string;
  division: string;
  played: string;
  win: string;
  draw: string;
  lose: string;
  for: string;
  against: string;
  goal_difference: string;
  points: string;
}
